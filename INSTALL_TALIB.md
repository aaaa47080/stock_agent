# Market Scanner 安裝指南

## 依賴庫（都是純 Python，無需編譯）

### 必需依賴
```bash
pip install pandas numpy matplotlib
```

### 推薦依賴（增強功能）

#### 1. pandas-ta（技術指標計算）
```bash
pip install pandas-ta
```

功能：提供豐富的技術指標計算（SMA, EMA, RSI, MACD, 布林帶等）

如果不安裝：會使用基礎的 pandas 計算，功能稍弱但足夠使用

#### 2. mplfinance（專業K線圖）
```bash
pip install mplfinance
```

功能：專業級K線圖繪製，帶成交量、指標疊加等

如果不安裝：會使用基礎的 matplotlib 繪製簡單K線圖

## 一鍵安裝所有依賴

```bash
pip install pandas numpy matplotlib pandas-ta mplfinance
```

## 驗證安裝

```python
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt

try:
    import pandas_ta as ta
    print("✓ pandas-ta installed")
except ImportError:
    print("✗ pandas-ta not installed (optional)")

try:
    import mplfinance as mpf
    print("✓ mplfinance installed")
except ImportError:
    print("✗ mplfinance not installed (optional)")
```

## 運行 Market Scanner

```bash
python analysis/market_scanner.py
```

## 功能特性

### 形態識別（純 Python 實現）
- ✓ 十字星 (Doji)
- ✓ 錘子 (Hammer) - 看漲信號
- ✓ 流星 (Shooting Star) - 看跌信號
- ✓ 吞噬形態 (Engulfing) - 看漲/看跌
- ✓ 三白兵 (Three White Soldiers) - 強烈看漲
- ✓ 三烏鴉 (Three Black Crows) - 強烈看跌

### 技術分析
- ✓ 趨勢分析（上升/下降/橫盤）
- ✓ 移動平均線（SMA20, SMA50, EMA12, EMA26）
- ✓ RSI 指標
- ✓ 支撐壓力位自動識別

### 圖表功能
- ✓ 專業K線圖（如果安裝 mplfinance）
- ✓ 移動平均線疊加
- ✓ 成交量顯示
- ✓ 支撐壓力位標註

## 優勢

相比 TA-Lib：
- ✅ **無需編譯** - 純 Python 實現
- ✅ **跨平台** - Windows/Linux/Mac 都可用
- ✅ **任何 Python 版本** - 無版本限制
- ✅ **安裝簡單** - pip install 即可
- ✅ **代碼透明** - 可自定義形態識別規則
- ✅ **降級友好** - 即使缺少可選庫也能運行

## 輸出示例

```
======================================================================
  MARKET SCANNER
  Lookback: 1000 candles
  pandas-ta: ✓ Enabled
  mplfinance: ✓ Enabled
======================================================================

──────────────────────────────────────────────────────────────────────
📊 BTC-USDT
──────────────────────────────────────────────────────────────────────

  [1h]
    Trend: UPTREND (2.45%)
    RSI: 58.3
    Resistance: 98500.00
    Support: 95200.00
    Patterns: 3 found
      • Hammer (錘子) (bullish)
      • Doji (十字星) (neutral)
      • Bullish Engulfing (看漲吞噬) (bullish)
    ✓ Chart: pattern_charts/BTC_USDT_1h.png
```

## 疑難排解

### 圖表顯示問題
如果圖表保存成功但無法打開，確認：
- 文件路徑是否正確
- 是否有讀取權限
- 圖片查看器是否支持 PNG 格式

### 中文顯示問題
如果圖表中文顯示為方框：
- Windows：確保系統有中文字體（默認都有）
- Linux：安裝中文字體包
- Mac：默認支持中文

### 性能問題
如果掃描速度慢：
- 減少 lookback 數量（默認1000）
- 減少掃描的交易對數量
- 減少時間週期數量
